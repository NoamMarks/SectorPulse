# SectorPulse pipeline runtime + dev dependencies
pandas>=2.2
numpy>=1.26
PyYAML>=6.0
jsonschema>=4.0
requests>=2.31

# Dev / test
pytest>=8.0

# Real-data providers.
# yfinance batch-downloads the full universe in one call (used for the daily EOD
# history seed — Tiingo's free tier caps ~50 unique symbols/hour, too low for a
# 177-symbol one-shot historical pull). Tiingo IEX is used for intraday live prices.
yfinance>=0.2.40
